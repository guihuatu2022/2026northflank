# 复制成 .env 供 `docker compose up` 使用。真正的值永远不要提交。
#
# NODE_ID:       uuidgen
# WS_PATH:       /assets/app.$(openssl rand -hex 16).js
# ORIGIN_SECRET: $(openssl rand -hex 24)

NODE_ID=
WS_PATH=
ORIGIN_SECRET=
LOG_LEVEL=debug
