{
  "name": "nf-node-edge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "wrangler deploy --dry-run --outdir /tmp/nf-node-edge-dryrun",
    "standalone": "node build-standalone.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
